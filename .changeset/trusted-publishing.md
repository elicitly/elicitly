---
"elicitly": patch
"@elicitly/tools": patch
---

Releases now publish via npm trusted publishing (GitHub Actions OIDC) with
automatic provenance — no long-lived npm token in CI. No runtime changes.
