---
"elicitly": patch
---

Add `mcpName` (`ai.elicitly/elicitly`) for Official MCP Registry ownership verification — the registry reads this field from the published npm artifact to prove the registry entry and the package share an owner.
